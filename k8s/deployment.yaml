# Kubernetes manifests for the XoXoPay Rust backend API
# Applies: Deployment, ClusterIP Service, ConfigMap
# Target: docker images built by CI (see .github/workflows)

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: xoxopay-backend-config
  namespace: default
  labels:
    app: xoxopay-backend
data:
  RUST_LOG: "info"
  PORT: "8080"
  # Set DATABASE_URL and other secrets via a separate Secret resource or
  # an external secrets operator (e.g. AWS Secrets Manager CSI driver).

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xoxopay-backend
  namespace: default
  labels:
    app: xoxopay-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: xoxopay-backend
  template:
    metadata:
      labels:
        app: xoxopay-backend
    spec:
      containers:
        - name: xoxopay-backend
          image: ghcr.io/fracverse/xoxopay-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: xoxopay-backend-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: xoxopay-backend-secrets
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: xoxopay-backend-secrets
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: xoxopay-backend
  namespace: default
  labels:
    app: xoxopay-backend
spec:
  type: ClusterIP
  selector:
    app: xoxopay-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
